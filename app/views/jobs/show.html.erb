<br>Title: <%= @job.title %>
<br>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

Company: <%= link_to @job.company.name, company_path(@job.company) %> </br>

Category: <%= link_to @job.category.title, category_path(@job.category) %> </br>

<%= render partial: 'comments/form' %>

<%= render partial: 'jobs/comment', collection: @job.comments.reverse %>

<%= link_to "Delete", job_path(@job), method: :delete, data: {confirm: "Really delete the job?"} %>

<p><%= link_to "Back to #{@job.company.name}", company_path(@job.company)%></p>
